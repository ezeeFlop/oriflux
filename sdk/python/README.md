# oriflux-sdk (Python)

ASGI middleware SDK with client-side 60 s aggregation (Apitally pattern). Ships to public
PyPI as `oriflux-sdk` under MIT. **Stub** — implemented by issue #8 (ASGI SDK + API
analytics pipeline).
